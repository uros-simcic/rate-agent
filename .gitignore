# Personal data never belongs in the engine repo: real watches, rate
# history, and the generated currency allowlist all live in a private
# instance repo (see README). Ignoring them here makes local test runs
# with a real config safe by default.
config.json
state.json
currencies.json

__pycache__/
*.pyc
.DS_Store
